# Datos locales del backend

El archivo `state.json` se genera automaticamente cuando arranca `backend/server.js`.

Este archivo guarda el estado local del MVP: organizaciones, respuestas de formularios, acciones, evidencias y eventos de auditoria.
